
class Point {
    constructor({ x, y }) {
        this.x = x;
        this.y = y;
    }

    add(b) {
        // TODO
    }

    static fromRadial(angle, radius) {
        // TODO
        return new Point({ x: 0, y: 0 });
    }

    static getRandom() {
        // TODO
        return new Point({ x: 0, y: 0 });
    }
}

module.exports = Point;
